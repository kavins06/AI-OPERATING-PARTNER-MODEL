interface:
  display_name: "Zero Trust Agent Controls"
  short_description: "Define least-privilege, observable, revocable controls for future agents."
  default_prompt: "Use this skill to design the zero-trust portion of the Step 15 risk_control_model: identity, scoped data access, tool permissions, output controls, approvals, logging, monitoring, incident response, revocation, and Step 13 loopback when controls force redesign."
