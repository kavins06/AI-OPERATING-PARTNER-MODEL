interface:
  display_name: "Technical Implementation Blueprint"
  short_description: "Design future architecture, runtime, hosting, access, truth remediation, normalization, tools, and build sequence without building."
  default_prompt: "Use this skill to create a Step 12 technical implementation blueprint with access paths, truth production remediation, runtime/orchestration requirements, hosting/environment requirements, identity resolution, normalization, tool contracts, test strategy, credentials/secrets plan, blocked paths, and future build sequence. Define requirements and acceptable patterns; do not prematurely select vendors, models, vector stores, or frameworks unless the client stack requires it."
